#!/bin/bash
# ExaPG CLI Wrapper - Convenience script for easy access
# Redirects to the main CLI script in scripts/cli/

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/scripts/cli/exapg" "$@" 