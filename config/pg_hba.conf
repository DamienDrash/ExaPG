# PostgreSQL Client Authentication Configuration File

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            trust

# IPv6 local connections:
host    all             all             ::1/128                 trust

# Allow connections from Docker network (change to md5 for production)
host    all             all             0.0.0.0/0               trust

# Allow connections between Citus nodes
host    all             postgres        coordinator             trust
host    all             postgres        worker1                 trust
host    all             postgres        worker2                 trust 